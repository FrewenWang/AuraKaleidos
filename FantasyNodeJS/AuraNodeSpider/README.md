# Aura Node Spider

可配置的 Node.js 爬虫骨架。源码按 `common/`、`database/`、`env/` 和 `spider/` 分层，入口为 `src/index.js`。

```bash
cp .env.example .env
npm install
npm test
npm start
```

`.env`、日志、下载结果和 `node_modules/` 不进入版本控制。

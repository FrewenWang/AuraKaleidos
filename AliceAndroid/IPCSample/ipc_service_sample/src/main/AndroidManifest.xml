<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frewen.ipc.service.sample">

    <permission
        android:name="com.frewen.ipc.service.sample.permission.REMOTE_BOOK_SERVICE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <service
            android:name=".service.RemoteBookManagerService"
            android:exported="true"
            android:permission="com.frewen.ipc.service.sample.permission.REMOTE_BOOK_SERVICE">
            <intent-filter>
                <action android:name="android.intent.service.REMOTE_BOOK_SERVICE" />
            </intent-filter>
        </service>

    </application>

</manifest>

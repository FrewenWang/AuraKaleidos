# hello_kotlin_android

Kotlin Android 入门工程，使用标准 Gradle/Android Studio 目录。业务代码与资源位于 `app/src/main`，
本地单测位于 `app/src/test`，设备测试位于 `app/src/androidTest`。

```bash
./gradlew test
./gradlew assembleDebug
```

# IPCSample

Android 多模块 IPC 示例：`app` 是客户端，`ipc_service` 提供服务实现，`ipc_service_sample` 展示服务接入。
工程保留 Gradle 标准的 `module/src/main`、`src/test` 和 `src/androidTest` 布局。

```bash
./gradlew test
./gradlew assembleDebug
```

# My Movie Web

基于 Express 和 Jade 的电影站点示例。

- `src/app.js`：Express 应用装配。
- `src/server.js`：服务进程入口。
- `src/routes/`：路由。
- `src/schemas/`：数据结构。
- `views/`、`public/`：页面模板与静态资源。
- `tests/`：HTTP 集成测试。

```bash
npm install
npm test
npm start
```

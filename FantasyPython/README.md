# FantasyPython

Python 独立工程集合。每个可安装工程使用 `pyproject.toml`，源码放在 `src/<package>/`，测试放在
`tests/`；教学脚本使用 `examples/`，Notebook 使用 `notebooks/`。

工具型工程（如 `aura-qdb`、`aura-midb`）保留 `bin/` 命令入口和 `scripts/` 安装脚本。

# Python Algorithm

Python 算法练习工程。可复用实现放在 `src/`，单元测试放在 `tests/`，项目元数据由 `pyproject.toml` 管理。

```bash
python -m pip install -e .
python -m unittest discover -s tests -v
```

带 `TODO` 的文件表示尚未完成的练习；文件名中带连字符的旧实现仅作为历史示例保留，不会打包为模块。

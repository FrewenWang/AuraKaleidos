# Phoenix自动化测试框架依赖
# 支持平台: Windows, macOS, Linux

# 基础依赖（所有平台）
certifi>=2018.11.29
chardet>=3.0.4
idna>=2.8
numpy>=1.17.2
psutil>=5.6.2
PyMySQL>=0.9.3
python-dateutil>=2.8.0
pytz>=2019.3
requests>=2.21.0
six>=1.12.0
urllib3>=1.24.1
websocket-client>=0.57.0
xlrd>=1.2.0
xlwt>=1.3.0
xlutils>=2.0.0

# 图像处理
opencv-python>=4.1.1.26

# GUI支持
PyQt5>=5.12.1

# MySQL连接
mysql-connector-python>=8.0.13

# 异步支持
gevent>=1.4.0
greenlet>=0.4.15

# 网络工具
beautifulsoup4>=4.9.0
lxml>=4.5.0

# 数据处理
pandas>=0.25.0

# 工具库
Pillow>=6.0.0
python-dotenv>=0.10.0

# ==================== 平台特定依赖 ====================

# Windows特定依赖（仅在Windows上安装）
# 取消注释以下依赖以支持Windows完整功能:
# pypiwin32==223; sys_platform == 'win32'
# pywin32==224; sys_platform == 'win32'
# WMI==1.4.9; sys_platform == 'win32'
# PyInstaller==3.5; sys_platform == 'win32'

# macOS特定依赖（仅在macOS上安装）
# pyobjc>=6.0; sys_platform == 'darwin'

# Linux特定依赖（仅在Linux上安装）
# 一般不需要额外的系统级依赖
